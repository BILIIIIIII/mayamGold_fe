"use client";

import { CardTitle } from "@/components/ui/card";

const BandaCardHeaderContent = ({}) => {
  return (
    <CardTitle className="text-left  text-4xl">
      Lhokseumawe Gold Price (IDR) per Mayam
    </CardTitle>
  );
};

export default BandaCardHeaderContent;
